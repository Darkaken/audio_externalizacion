
def new_status(json_data):

    #Esta funcion recibe como input:

    # json_data = {
    #   "node_id": int,
    #   "db_verification_hash": str
    # }

    #Esta funcion toma el node_id y lo registra en la base de datos como disponible para asignacion de audios
    #Tambien tiene que revisar si la base de datos del nodo esta al dia con la del nodo central (se usa el hash para eso)

    #Tiene que retornar una actualizacion de la base de datos y un mensaje de exito / errores

    pass
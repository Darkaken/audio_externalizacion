
def return_audio(json_data):

    # Esta funcion toma como input:

    # json_data = {
    #   "node_id": int,
    #   "result": int        # ID de tanda encontrada, 0 si no se encuentra una tanda
    # }

    # Esta funcion deberia hacer 2 cosas:

    # 1) Si es que se encuentra una tanda se deberia registrar en una base de datos este evento
    #    Para esto se usa primero el NewBlock para agregar este evento al blockchain

    # 2) En cualquier caso, se tienen que actualizar las estadisticas del nodo:
    #    nodo.audios_procesados += 1
    #    nodo.tandas_encontradas += 1 (si se encuentra una tanda)

    pass